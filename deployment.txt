Jolitos puslapio deploymentas: 


git checkout main
npm install
npm run build

Remove-Item -Recurse -Force tempDeploy -ErrorAction Ignore
Copy-Item dist tempDeploy -Recurse


{
jeigu kas nors pvz deployment.txt. buvo pakeistas 
git add public/deployment.txt
git commit -m "update deployment file"
git checkout deploy}

git checkout deploy

Get-ChildItem -Exclude ".git","tempDeploy" | Remove-Item -Recurse -Force
Copy-Item tempDeploy\* . -Recurse -Force

git add .
git commit -m "deploy"
git push origin deploy


# Plesk:  ( jungtis prei J telia po to prisijungti prie https://hosting.telia.lt/
 po prie plesk  )


Git settings → set branch = deploy

Deployment path = /httpdocs

Click Pull Now




# Po deploymento gali tekti taisyti dependancies: 

npm install
npm run dev
